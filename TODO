Jquery referenced statically and not over the internet
Don't forget to replace 'http://localhost:3000/auth/login' with your actual server's address and the route where you handle login requests. (in login.js)
call closeDatabase function in an appropriate place in your application, such as a shutdown or cleanup hook.:
Download Jquery and jwidget scripts and move them to a folder. Change paths everywhere needed.
Maybe use AUTOINCREMENT on pks