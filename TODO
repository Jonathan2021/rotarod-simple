Titre pour les Runs:
    - Ajouter une colonne dans la database et migrer les entrées existantes.
    - Ajouter un champ dans le formulaire de modification du Run avec le titre
    - Afficher le titre à gauche de la date dans le menu principal dans la liste des runs.


OPTIONAL:
Revenir au menu principal garde la sélection d'avant (par example t'ouvres un run puis tu reviens au menu, les mêmes choses doivent être sélectionnées)
Moyenne par groupe de traitement (groupe (zigosity) + traitement) dans le excel aggregé.
Rendre la taille de la grille pour encoder les temps (dans le formulaire pour les Runs) adaptable en hauteur pour que si la fenêtre est rapetissée, les boutons sont toujours visibles et la hauteur de la grille s'adapte (scroll).
Plutôt que de sauvegarder tout à chaque modification (dans la page de modification d'un run), faire des requête pour la modification de l'objet en question uniquement (plus propre et efficace mais la solution actuelle marche bien)